import Foundation

/// External support/branding links (#68 §3.9).
enum SupportLinks {
    /// The Ko-fi page. PLACEHOLDER: no Ko-fi handle exists in
    /// the repo yet — swap in the real URL before release
    /// (one constant, two surfaces: quick menu + About).
    static let koFi = URL(
        string: "https://ko-fi.com/kiwidesk"
    )!
}
