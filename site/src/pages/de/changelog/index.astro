---
// KiwiDesk — German release notes at /de/changelog/.
//
// The page lives entirely in the shared <Changelog> component;
// this route only supplies the locale. Entries come from
// src/data/changelog.json (generated — see scripts/changelog-sync).
import Changelog from "../../../components/Changelog.astro";
---

<Changelog lang="de" />
