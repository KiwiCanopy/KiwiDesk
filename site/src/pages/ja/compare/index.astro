---
// KiwiDesk — Japanese comparison page at /ja/compare/ (#975).
//
// Renders the same shared <Compare> component as the English
// route, with the Japanese locale. Untranslated keys fall back to
// English per key (see Compare.astro), so this route is safe
// before ja.json's compare_* keys land.
import Compare from "../../../components/Compare.astro";
---

<Compare lang="ja" />
