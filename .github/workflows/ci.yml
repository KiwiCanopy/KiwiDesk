name: CI

# push runs post-merge on main; pull_request runs pre-merge on
# branches. Scoping push to main avoids a duplicate run on every
# PR-branch push (which would otherwise fire both push and
# pull_request for the same commit).
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # Manual "Run workflow" button in the Actions tab. Additive — the
  # automatic triggers above still fire. Useful while the repo is
  # private, where macOS runners bill at a 10x multiplier against a
  # 2,000-minute allowance; on a public repo Actions is free and this
  # is just a convenience for re-running without an empty commit.
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-lint-test:
    name: Build, Lint & Test
    # macos-26 (Tahoe), not macos-15: the tree uses macOS 26 SDK
    # symbols (NSGlassEffectView, #390). `if #available` gates
    # execution, not compilation, so the symbol must exist in the
    # SDK or the file cannot compile at all. The deployment target
    # is unchanged, so the binary still runs on older macOS.
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode_26.6.app \
            || sudo xcode-select -s /Applications/Xcode.app
          swift --version

      - name: Build
        run: swift build

      - name: Lint
        run: ./scripts/lint.sh

      # Split per AGENTS.md §5: run as one `swift test` the suite
      # stalls for minutes at the tail, because spawned exec
      # children hold the runner's pipe. #489 tracks the root fix.
      - name: Test
        run: swift test --skip ExecTests

      - name: Test (ExecTests)
        run: swift test --filter ExecTests

  release-build:
    name: Release Build
    # A sibling job, not a fourth step in build-lint-test: `swift
    # test` there already compiles the tree in debug, so building
    # release in the same job compiles it twice in series and
    # grows the pipeline by the release build's full wall clock.
    # As a parallel job it costs nothing on the critical path.
    #
    # This is the gate AGENTS.md §3 no longer asks every commit to
    # pay locally: the optimizer turns on concurrency diagnostics
    # (non-Sendable captures in `@Sendable` closures) the debug
    # build silently accepts. #532.
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode_26.6.app \
            || sudo xcode-select -s /Applications/Xcode.app
          swift --version

      - name: Build (release)
        run: swift build -c release
